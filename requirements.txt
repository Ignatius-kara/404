# Core framework
streamlit>=1.28.0

# For JSON handling (built-in, but listing for completeness)
# json - built-in Python module

# For random responses (built-in, but listing for completeness)  
# random - built-in Python module

# Transformers and ML dependencies
transformers>=4.30.0
torch>=2.0.0
tokenizers>=0.13.0

# Optional: If you want to use specific torch versions for CPU/GPU
# For CPU only (uncomment if needed):
# torch>=2.0.0+cpu -f https://download.pytorch.org/whl/torch_stable.html

# For CUDA support (uncomment if needed):
# torch>=2.0.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html

# Additional dependencies that might be needed
numpy>=1.21.0
requests>=2.28.0

# Optional: For better performance and caching
# streamlit-chat>=0.1.1  # If you want to use streamlit-chat components
# streamlit-option-menu>=0.3.6  # For better navigation menus
